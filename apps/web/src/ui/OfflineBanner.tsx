/** Этап 9 — понятное игроку (не техническое) сообщение об отсутствии сети,
 * когда есть облачный аккаунт (см. App.tsx — рендерится только тогда). Без
 * облака действия чисто локальные, офлайн ничему не мешает и баннер не нужен. */
export function OfflineBanner() {
  return (
    <div className="offline-banner" role="status">
      📡 Нет соединения — действия сохраняются и досинхронизируются, когда сеть вернётся.
    </div>
  );
}
