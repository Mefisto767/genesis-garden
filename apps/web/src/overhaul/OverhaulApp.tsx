// Временная заглушка на время сборки фича-флага (Задача 3 плана).
// Реальный EstateScene/LaboratoryScene подключаются следующими коммитами
// этой же ветки — см. docs/ASSET_MANIFEST.md и docs/ARCHITECTURE.md
// (раздел Visual Overhaul) по мере готовности.
export function OverhaulApp() {
  return (
    <div style={{ padding: 24, fontFamily: 'sans-serif' }}>
      Visual Overhaul: сцены в разработке в этой же ветке.
    </div>
  );
}
